spring:
  jpa:
    properties:
      hibernate:
        default_schema: public
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true
    contexts: lcl
    default-schema: public
  datasource:
    hikari:
      maximum-pool-size: 10